<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.spring_boot_board.mapper.BoardMapper">

    <insert id="createBoard" parameterType="com.example.spring_boot_board.domain.Board">
        INSERT INTO board (title, content, writer, registered)
        VALUES (#{title}, #{content}, #{writer}, #{registered})
    </insert>

    <select id="readBoard" parameterType="int" resultType="com.example.spring_boot_board.domain.Board">
        SELECT id, title, content, writer, registered
        FROM board
        WHERE id = #{id}
    </select>

    <update id="updateBoard" parameterType="com.example.spring_boot_board.domain.Board">
        UPDATE board
        SET title = #{title},
        content = #{content},
        writer = #{writer},
        registered = #{registered}
        WHERE id = #{id}
    </update>

    <delete id="deleteBoard" parameterType="int">
        DELETE FROM board
        WHERE id = #{id}
    </delete>

    <select id="getAllBoards" parameterType="string" resultType="com.example.spring_boot_board.domain.Board">
        SELECT id, title, content, writer, registered
        FROM board
        where id like %#{writer}%
    </select>
</mapper>